<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create a question</title>
    <link href="stylesheets/question.css" rel="stylesheet" />
  </head>
  <body>
    <h2>CSS Template using Grid</h2>
    <p>
      In this example, we have created a header, three unequal columns and a
      footer. On smaller screens, the columns will stack on top of each other.
    </p>
    <p>Resize the browser window to see the responsive effect.</p>
    <p>
      <strong>Note:</strong> The Grid Layout Module is not supported in Internet
      Explorer or Edge 15 eand earlier versions.
    </p>

    <div class="grid-container">
      <div class="header">
        <h2>Header</h2>
      </div>

      <div class="left" style="background-color: #aaa">Column</div>
      <div class="middle" style="background-color: #bbb">
        <!-- <form action=""> -->
        <div>Question <textarea name="questionBox" id=""></textarea></div>

        Options

        <div id="let"></div>
        <button id="addoption" onclick="changeText(this)">Add option</button>

        <h1 onclick="changeText(this)">Click on this text!</h1>

        <!-- </form> -->
      </div>
      <div class="right" style="background-color: #ccc">Column</div>

      <div class="footer">
        <p>Footer</p>
      </div>
    </div>
    <script>
      const newlength = 1;
      document.getElementById("addoptions").onclick = newlength++;

      function changeText(id) {
        alert("clicked");
        document.getElementById("let").innerHTML =
          `<input type="text" id="optionA"/>`;
        // id.innerHTML = "Ooops!";
      }

      function onclick(id) {
        //console.log("clicked me");
        alert("clicked");
        document.getElementById("let").innerHTML =
          2 * `<input type="text" id="optionA"/>`;
      }
    </script>
  </body>
</html>
